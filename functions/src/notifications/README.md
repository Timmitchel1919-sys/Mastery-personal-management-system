# functions/src/notifications

Push notification delivery (Firebase Cloud Messaging), token lifecycle, quiet hours,
duplicate prevention — **Layer 17**. Recovery notifications use privacy-safe wording and a
separate channel. See `docs/PRODUCT_REQUIREMENTS.md` §12.
