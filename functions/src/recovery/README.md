# functions/src/recovery

Recovery Center server-mediated access — **Layer 15**. `recoveryCoachQuery` (isolated
system prompt + context), relapse/coach-session writes, and accountability-partner access
(scoped projections only). Never mixed with the general AI. See `docs/RECOVERY_PRIVACY.md`.
