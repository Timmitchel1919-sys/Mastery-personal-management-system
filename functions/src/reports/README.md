# functions/src/reports

Server-controlled report + PDF generation — **Layer 16**. Uses authenticated user data,
never includes Recovery Center content automatically. See `docs/PRODUCT_REQUIREMENTS.md` §11.
